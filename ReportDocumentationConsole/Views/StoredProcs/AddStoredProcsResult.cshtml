
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>AddStoredProcsResult</title>
</head>
<body>
    <center>
        <div>
            Success!
            @using (Html.BeginForm("Index", "StoredProcs", FormMethod.Post))
            {
                @Html.Hidden("selectedReportId", @ViewData["selectedReportId"])
                @Html.Hidden("selectedReportName", @ViewData["selectedReportName"])
                <input type="submit" value="Return To StoredProcs" class="btn btn-primary" />
            }

        </div>
    </center>
</body>
</html>
